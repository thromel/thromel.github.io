---
phase: 02-homepage-hierarchy
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - index.html
  - assets/css/overhaul.css
  - assets/css/components/oss-summary.css
autonomous: true
requirements:
  - HOME-02
  - HOME-03
user_setup: []
must_haves:
  truths:
    - "The homepage no longer jumps directly from the hero into three competing support surfaces without orientation."
    - "Visitors can jump to the major homepage sections through explicit section pills."
    - "Current focus, OSS proof, and news read as a calmer support sequence without redesigning OSS runtime behavior."
  artifacts:
    - path: "index.html"
      provides: "Homepage support-band markup with section IDs and section-pill navigation"
      contains: "home-section-nav"
    - path: "assets/css/overhaul.css"
      provides: "Homepage support-band and wayfinding spacing rules"
      contains: ".homepage-classic .home-support-stack"
    - path: "assets/css/components/oss-summary.css"
      provides: "OSS summary spacing that fits the intro-support band"
      contains: ".oss-inline"
  key_links:
    - from: "index.html"
      to: "assets/js/site-shell.js"
      via: "homepage section pills use the shell's existing active-section behavior"
      pattern: "home-section-nav"
    - from: "index.html"
      to: "assets/css/components/oss-summary.css"
      via: "the OSS summary remains behaviorally unchanged but sits inside the new support stack"
      pattern: "oss-summary"
---

<objective>
Simplify homepage action density and pacing after the hero by adding explicit wayfinding and a calmer support sequence.

Purpose: Even with a better hero, the homepage will still feel rushed if current focus, GitHub proof, news, and the long-form sections all compete without a clear transition.
Output: `index.html`, `assets/css/overhaul.css`, and `assets/css/components/oss-summary.css` create a support band and explicit section navigation while leaving Phase 4 async behavior untouched.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-homepage-hierarchy/02-RESEARCH.md
@index.html
@assets/css/overhaul.css
@assets/css/components/oss-summary.css
@assets/js/site-shell.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a homepage support stack and section-pill wayfinding for the major homepage destinations</name>
  <files>index.html, assets/css/overhaul.css</files>
  <read_first>index.html, assets/css/overhaul.css, assets/js/site-shell.js, .planning/phases/02-homepage-hierarchy/02-RESEARCH.md</read_first>
  <action>Refactor the homepage markup in `index.html` so the post-hero support surfaces are grouped into a `home-support-stack` immediately after the hero. Keep `current-focus` as the first support surface, add a `nav` with class `home-section-nav` and `.section-pill` links for `News`, `Publications`, `Research`, `Work`, `Projects`, and `Skills`, and add matching IDs to the destination sections using the `homepage-` prefix: `homepage-news`, `homepage-publications`, `homepage-research`, `homepage-work`, `homepage-projects`, and `homepage-skills`. Keep the existing section content and copy intact. Add homepage-specific spacing rules in `assets/css/overhaul.css` so the support stack reads as one calmer sequence rather than three disconnected attention spikes.</action>
  <verify>rg -n "home-support-stack|home-section-nav|section-pill|id=\"homepage-(news|publications|research|work|projects|skills)\"" index.html assets/css/overhaul.css</verify>
  <acceptance_criteria>
    - `index.html` contains `home-support-stack`
    - `index.html` contains `home-section-nav`
    - `index.html` contains `section-pill`
    - `index.html` contains `id="homepage-news"`
    - `index.html` contains `id="homepage-publications"`
    - `index.html` contains `id="homepage-research"`
    - `index.html` contains `id="homepage-work"`
    - `index.html` contains `id="homepage-projects"`
    - `index.html` contains `id="homepage-skills"`
    - `assets/css/overhaul.css` contains `.homepage-classic .home-support-stack`
  </acceptance_criteria>
  <done>The homepage has explicit wayfinding and stable anchor targets for the main content sections.</done>
</task>

<task type="auto">
  <name>Task 2: Reposition the OSS summary and news widget into the calmer support sequence without changing GitHub runtime logic</name>
  <files>index.html, assets/css/overhaul.css, assets/css/components/oss-summary.css</files>
  <read_first>index.html, assets/css/overhaul.css, assets/css/components/oss-summary.css, assets/js/oss-summary.js</read_first>
  <action>Adjust the homepage support-zone markup so the existing `oss-summary` sits inside the new post-hero support sequence rather than floating as a separate interruption before the news widget. Keep the `oss-summary` element ID, its `data-github-user` attribute, and the existing `assets/js/oss-summary.js` behavior unchanged. Update `assets/css/overhaul.css` and `assets/css/components/oss-summary.css` only as needed to remove the old standalone spacing assumptions and make the OSS badge line read as a quieter proof row between the intro support band and the news widget. Do not add loading states, change fetch logic, or redesign the proof copy in this plan.</action>
  <verify>rg -n "oss-summary|oss-inline|news-widget|home-support-stack" index.html assets/css/overhaul.css assets/css/components/oss-summary.css assets/js/oss-summary.js</verify>
  <acceptance_criteria>
    - `index.html` contains `id="oss-summary"`
    - `index.html` contains `news-widget`
    - `assets/js/oss-summary.js` still contains `container.style.display = 'flex'`
    - `assets/css/components/oss-summary.css` contains `.oss-inline`
    - `assets/css/overhaul.css` contains `.homepage-classic .news-widget`
    - `assets/css/overhaul.css` contains `.homepage-classic .home-support-stack`
  </acceptance_criteria>
  <done>The homepage intro support surfaces have a calmer sequence and the OSS proof remains behaviorally unchanged.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "home-section-nav|section-pill|homepage-news|homepage-publications|homepage-research|homepage-work|homepage-projects|homepage-skills" index.html` shows the explicit wayfinding structure
- [ ] `rg -n "oss-summary|news-widget|home-support-stack" index.html assets/css/overhaul.css assets/css/components/oss-summary.css` shows the support-sequence structure
- [ ] `assets/js/oss-summary.js` remains unchanged in behavior-critical lines (`fetchCount`, `container.style.display = 'flex'`)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Homepage support surfaces read as one calmer sequence rather than a pileup of competing blocks
- Visitors can jump directly from the intro to the major homepage sections
  </success_criteria>

<output>
After completion, create `.planning/phases/02-homepage-hierarchy/02-02-SUMMARY.md`
</output>
