---
phase: 02-homepage-hierarchy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - assets/css/overhaul.css
  - assets/css/mobile-optimizations.css
autonomous: true
requirements:
  - HOME-01
  - HOME-02
user_setup: []
must_haves:
  truths:
    - "The homepage hero leads with name, message, and supporting copy before the portrait on mobile."
    - "The first screen exposes one small primary action row and a quieter secondary contact row instead of a flat action cluster."
    - "The homepage hero reuses the repo's existing intro/action vocabulary instead of inventing another competing homepage system."
  artifacts:
    - path: "index.html"
      provides: "Intro-first homepage hero markup with primary and secondary action lanes"
      contains: "hero-primary-actions"
    - path: "assets/css/overhaul.css"
      provides: "Homepage classic hero styling for the reduced action hierarchy"
      contains: ".homepage-classic .hero-primary-actions"
    - path: "assets/css/mobile-optimizations.css"
      provides: "Mobile hero rules that keep content ahead of the image"
      contains: ".hero-section"
  key_links:
    - from: "index.html"
      to: "assets/css/overhaul.css"
      via: "homepage hero classes share the same intro/action vocabulary already used elsewhere in the repo"
      pattern: "hero-primary-actions|hero-contact-row"
    - from: "assets/css/mobile-optimizations.css"
      to: "index.html"
      via: "mobile hero order preserves text-first reading before the portrait"
      pattern: "hero-image-wrapper"
---

<objective>
Rework the homepage hero so the message leads, the portrait supports it, and the action pattern becomes simpler.

Purpose: Phase 2 cannot improve homepage hierarchy while the first screen still promotes the portrait before the copy on mobile and presents every outbound path at the same visual weight.
Output: `index.html`, `assets/css/overhaul.css`, and `assets/css/mobile-optimizations.css` make the homepage hero intro-first and visibly calmer.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-homepage-hierarchy/02-RESEARCH.md
@index.html
@assets/css/overhaul.css
@assets/css/mobile-optimizations.css
@research.html
@projects.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the homepage hero cluster with an intro-first content stack and split action lanes</name>
  <files>index.html, assets/css/overhaul.css</files>
  <read_first>index.html, assets/css/overhaul.css, research.html, projects.html, .planning/phases/02-homepage-hierarchy/02-RESEARCH.md</read_first>
  <action>Refactor the homepage hero in `index.html` so the content block includes a small kicker, the existing name and tagline, the two current biography paragraphs, and then two separate action rows: a `hero-primary-actions` row containing only the existing `Research` link and CV download, and a `hero-contact-row` containing the existing email link, optional meeting link, and the existing social/profile destinations. Keep `More about me` inline with the secondary supporting copy, keep the portrait as a supporting visual in the second hero column on desktop, and reuse the existing intro/action vocabulary from the repo (`hero-primary-actions`, `hero-contact-row`, `hero-kicker`, `page-intro`-style spacing) instead of introducing a new homepage-only naming scheme. Add or update homepage-specific rules in `assets/css/overhaul.css` so the primary action row is visually stronger than the contact row and the old flat `.hero-actions` cluster no longer drives the first-screen layout.</action>
  <verify>rg -n "hero-kicker|hero-primary-actions|hero-contact-row|/assets/pdf/cv\\.pdf|href=\\\"\\{\\{ 'research' \\| relative_url \\}\\}\\\"" index.html assets/css/overhaul.css</verify>
  <acceptance_criteria>
    - `index.html` contains `hero-kicker`
    - `index.html` contains `hero-primary-actions`
    - `index.html` contains `hero-contact-row`
    - `index.html` contains `/assets/pdf/cv.pdf`
    - `index.html` contains `href="{{ 'research' | relative_url }}"`
    - `assets/css/overhaul.css` contains `.homepage-classic .hero-primary-actions`
    - `assets/css/overhaul.css` contains `.homepage-classic .hero-contact-row`
  </acceptance_criteria>
  <done>The homepage hero exposes a clear narrative stack, a short primary action row, and a quieter secondary contact row.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the mobile portrait-first override and tune the hero for text-first scanning on phones</name>
  <files>assets/css/mobile-optimizations.css, assets/css/overhaul.css</files>
  <read_first>assets/css/mobile-optimizations.css, assets/css/overhaul.css, index.html</read_first>
  <action>Update the mobile hero rules so `.hero-image-wrapper` no longer uses `order: -1`, and the homepage keeps the content block visually ahead of the portrait on phone widths. Retain the single-column hero on mobile, but tune the homepage-specific spacing so the content stack, primary action row, and secondary contact row do not collapse into one centered block. Keep the portrait smaller than desktop and supporting rather than leading. Do not change shared shell/mobile nav rules unrelated to the homepage hero.</action>
  <verify>rg -n "hero-image-wrapper|hero-primary-actions|hero-contact-row|hero-section" assets/css/mobile-optimizations.css assets/css/overhaul.css && ! rg -n "order:\\s*-1" assets/css/mobile-optimizations.css</verify>
  <acceptance_criteria>
    - `assets/css/mobile-optimizations.css` contains `.hero-section`
    - `assets/css/mobile-optimizations.css` contains `.hero-image-wrapper`
    - `assets/css/mobile-optimizations.css` does not contain `order: -1`
    - `assets/css/overhaul.css` contains `@media (max-width: 640px)` or `@media (max-width: 768px)`
    - `assets/css/overhaul.css` contains `.homepage-classic .hero-primary-actions`
  </acceptance_criteria>
  <done>The homepage keeps its message ahead of the portrait on mobile and remains readable after the action split.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "hero-kicker|hero-primary-actions|hero-contact-row" index.html assets/css/overhaul.css` shows the new hero hierarchy
- [ ] `rg -n "order:\\s*-1" assets/css/mobile-optimizations.css` returns no matches
- [ ] `index.html` still contains the homepage portrait, research link, CV link, email link, and optional meeting link in the refactored hero
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The homepage headline and copy lead before the portrait on mobile
- The hero exposes fewer equally weighted first-screen exits than the current action cluster
  </success_criteria>

<output>
After completion, create `.planning/phases/02-homepage-hierarchy/02-01-SUMMARY.md`
</output>
