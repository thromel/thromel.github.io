---
phase: 05-verification-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-ui.sh
  - tests/shell-behavior.spec.js
autonomous: true
requirements:
  - QUAL-01
user_setup: []
must_haves:
  truths:
    - "Maintainers have one repo-local command path that rebuilds, serves, and runs the UI regression suites without relying on memory."
    - "Shared shell regressions cover the remaining core desktop/mobile behaviors still missing from automated coverage."
    - "Phase 5 keeps the current lightweight local-tooling model and does not introduce a committed Node manifest or CI dependency."
  artifacts:
    - path: "scripts/verify-ui.sh"
      provides: "Repo-local UI verification runner for shell and full regression modes"
      contains: "npx playwright test"
    - path: "tests/shell-behavior.spec.js"
      provides: "Expanded canonical shell regression coverage for shared responsive behaviors"
      contains: "backToTop"
  key_links:
    - from: "scripts/verify-ui.sh"
      to: "tests/shell-behavior.spec.js"
      via: "shell mode runs the shared shell regression suite through one repo-local entrypoint"
      pattern: "shell-behavior.spec.js"
    - from: "tests/shell-behavior.spec.js"
      to: "_layouts/default.html"
      via: "shell assertions continue to target the canonical shared layout controls"
      pattern: "themeToggle|site-navigation|mobileNavDrawer|scrollProgress|backToTop"
---

<objective>
Make the repo's shell/browser verification repeatable through one repo-local entrypoint and close the biggest remaining shared shell regression gaps.

Purpose: Phase 5 starts by making the verification path easy to run and broad enough to catch the remaining shared shell regressions.
Output: `scripts/verify-ui.sh` and an expanded `tests/shell-behavior.spec.js` give maintainers a repeatable shell verification baseline.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-verification-hardening/05-RESEARCH.md
@.planning/phases/05-verification-hardening/05-VALIDATION.md
@manual-testing-script.md
@tests/shell-behavior.spec.js
@tests/navbar-layout.spec.js
@tests/homepage-hierarchy.spec.js
@tests/readability-hierarchy.spec.js
@tests/proof-surfaces.spec.js
@browser-console-tests.js
@_layouts/default.html
@_includes/navbar.html
@_includes/footer.html
@assets/js/site-shell.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a repo-local UI verification runner that packages the current Jekyll + Playwright flow</name>
  <files>scripts/verify-ui.sh</files>
  <read_first>manual-testing-script.md, README.md, .planning/phases/05-verification-hardening/05-RESEARCH.md</read_first>
  <action>Create `scripts/verify-ui.sh` as the repo-local verification entrypoint for UI checks. The script should build the site with the currently required Homebrew Ruby 3.3 path, serve `_site` locally, and run Playwright suites through at least `shell` and `full` modes. It should trap and clean up the local server process, fail clearly when `@playwright/test` is missing instead of auto-installing it, and keep the repo on the current lightweight tooling model rather than introducing a committed package manifest.</action>
  <verify>rg -n "bundle exec jekyll build|python3 -m http.server|npx playwright test|case \\\"\\$MODE\\\"|shell|full" scripts/verify-ui.sh</verify>
  <acceptance_criteria>
    - `scripts/verify-ui.sh` exists
    - `scripts/verify-ui.sh` contains `bundle exec jekyll build`
    - `scripts/verify-ui.sh` contains `python3 -m http.server`
    - `scripts/verify-ui.sh` contains `npx playwright test`
    - `scripts/verify-ui.sh` contains `shell`
    - `scripts/verify-ui.sh` contains `full`
  </acceptance_criteria>
  <done>The repo has one repeatable command path for UI regression checks.</done>
</task>

<task type="auto">
  <name>Task 2: Expand shared shell regression coverage for the remaining core responsive behaviors</name>
  <files>tests/shell-behavior.spec.js</files>
  <read_first>tests/shell-behavior.spec.js, manual-testing-script.md, browser-console-tests.js, _layouts/default.html, _includes/navbar.html, _includes/footer.html, assets/js/site-shell.js</read_first>
  <action>Expand `tests/shell-behavior.spec.js` so the canonical shell suite covers the remaining shared behaviors still missing from automated coverage. Include `Learning` as another representative shared-shell page, keep the current theme-toggle and mobile-drawer assertions, and add coverage for shared shell behaviors such as skip links, scroll progress, and back-to-top behavior on desktop/mobile where they make sense. Keep the suite focused on shared shell mechanics and do not duplicate homepage hierarchy or proof-surface assertions already owned by other test files.</action>
  <verify>node -c tests/shell-behavior.spec.js && rg -n "Learning|skip-link|scrollProgress|backToTop|mobile drawer|theme toggle" tests/shell-behavior.spec.js</verify>
  <acceptance_criteria>
    - `tests/shell-behavior.spec.js` contains `Learning`
    - `tests/shell-behavior.spec.js` contains `skip-link`
    - `tests/shell-behavior.spec.js` contains `scrollProgress`
    - `tests/shell-behavior.spec.js` contains `backToTop`
    - existing theme-toggle and mobile-drawer coverage remains in the file
    - `node -c tests/shell-behavior.spec.js` exits 0
  </acceptance_criteria>
  <done>The canonical shell suite covers the remaining high-signal shared shell behaviors in a repeatable browser test.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `scripts/verify-ui.sh` exists and contains build/serve/test orchestration for shell and full modes
- [ ] `node -c tests/shell-behavior.spec.js` exits 0
- [ ] `rg -n "Learning|skip-link|scrollProgress|backToTop" tests/shell-behavior.spec.js` shows the expanded shell coverage
- [ ] `bash scripts/verify-ui.sh shell` passes after the implementation lands
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The repo has a repeatable shell-focused verification command
- Shared shell/browser coverage extends beyond theme toggle and drawer open/close
  </success_criteria>

<output>
After completion, create `.planning/phases/05-verification-hardening/05-01-SUMMARY.md`
</output>
