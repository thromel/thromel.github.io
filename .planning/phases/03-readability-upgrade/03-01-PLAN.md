---
phase: 03-readability-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/overhaul.css
  - assets/css/mobile-optimizations.css
autonomous: true
requirements:
  - READ-01
  - READ-02
user_setup: []
must_haves:
  truths:
    - "Dense reading surfaces no longer default to 12px to 13px body copy."
    - "Mobile dense content inherits a comfortable reading floor instead of compressed desktop-era sizes."
    - "The site still reads as academic and restrained, with metadata remaining subordinate to body copy."
  artifacts:
    - path: "assets/css/overhaul.css"
      provides: "Retuned shared text scale and line-height floor for dense content"
      contains: "--text-base"
    - path: "assets/css/mobile-optimizations.css"
      provides: "Mobile dense-surface typography overrides aligned to the new readability floor"
      contains: ".timeline-description"
  key_links:
    - from: "assets/css/overhaul.css"
      to: "index.html"
      via: "homepage timelines, news, projects, and current-focus cards inherit the shared reading scale"
      pattern: "timeline-description|news-description|project-description|current-focus-card"
    - from: "assets/css/mobile-optimizations.css"
      to: "learning.html"
      via: "mobile dense-content overrides protect learning, project, and timeline readability on phone widths"
      pattern: "project-description|skill-item|timeline-description"
---

<objective>
Retune the shared typography floor so dense content reads comfortably before page-local readability fixes begin.

Purpose: Phase 3 cannot succeed if the shared text scale still bottoms out at metadata-sized values for the site’s primary reading surfaces.
Output: `assets/css/overhaul.css` and `assets/css/mobile-optimizations.css` establish a more comfortable desktop and mobile reading baseline without turning the site into a large-type redesign.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-readability-upgrade/03-RESEARCH.md
@assets/css/overhaul.css
@assets/css/mobile-optimizations.css
@index.html
@learning.html
@research.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise the shared reading floor and retune dense-copy line heights in the canonical stylesheet</name>
  <files>assets/css/overhaul.css</files>
  <read_first>assets/css/overhaul.css, .planning/phases/03-readability-upgrade/03-RESEARCH.md, index.html, learning.html</read_first>
  <action>Update `assets/css/overhaul.css` so the lower end of the shared text scale no longer forces dense reading surfaces into 12px to 13px body copy. Retune the relevant shared typography tokens and nearby supporting rules (`p`, `.abstract`, `.section-subtitle`, and shared dense-surface selectors that are meant to read as body/support copy) so the site gains a more comfortable reading floor while preserving the current academic tone and keeping metadata clearly subordinate. Do not redesign color, card structure, or homepage information architecture in this task.</action>
  <verify>rg -n -- "--text-sm:|--text-base:|--text-lg:|--leading-relaxed:|\\.section-subtitle|\\.timeline-description|\\.news-description|\\.project-description" assets/css/overhaul.css</verify>
  <acceptance_criteria>
    - `assets/css/overhaul.css` contains `--text-sm`
    - `assets/css/overhaul.css` contains `--text-base`
    - `assets/css/overhaul.css` contains `--text-lg`
    - `assets/css/overhaul.css` contains `.section-subtitle`
    - `assets/css/overhaul.css` contains `.timeline-description`
    - `assets/css/overhaul.css` contains `.news-description`
    - `assets/css/overhaul.css` contains `.project-description`
  </acceptance_criteria>
  <done>The shared stylesheet establishes a denser-content reading floor that is visibly more comfortable without flattening the hierarchy.</done>
</task>

<task type="auto">
  <name>Task 2: Align mobile dense-content overrides to the new floor without creating oversized cards</name>
  <files>assets/css/mobile-optimizations.css, assets/css/overhaul.css</files>
  <read_first>assets/css/mobile-optimizations.css, assets/css/overhaul.css, index.html, learning.html</read_first>
  <action>Update `assets/css/mobile-optimizations.css` so the phone-width typography for timelines, descriptions, projects, learning/resource surfaces, and skill-heavy cards inherits the new readability floor and does not fall back to cramped values. Keep the current responsive layout model, but adjust mobile font sizes and spacing where needed so dense cards remain readable without becoming bulky. Do not touch shell/mobile nav behavior or Phase 2 homepage hierarchy structure in this task.</action>
  <verify>rg -n "timeline-description|timeline-subtitle|project-description|skill-item|resource-description|learning-section-copy" assets/css/mobile-optimizations.css assets/css/overhaul.css</verify>
  <acceptance_criteria>
    - `assets/css/mobile-optimizations.css` contains `.timeline-description`
    - `assets/css/mobile-optimizations.css` contains `.timeline-subtitle`
    - `assets/css/mobile-optimizations.css` contains `.project-description`
    - `assets/css/mobile-optimizations.css` contains `.skill-item`
    - `assets/css/overhaul.css` contains `@media (max-width: 768px)` or `@media (max-width: 640px)`
  </acceptance_criteria>
  <done>Dense mobile content stays readable after the shared scale change and does not regress into cramped or oversized cards.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n -- "--text-sm:|--text-base:|--text-lg:" assets/css/overhaul.css` shows the retuned shared floor
- [ ] `rg -n "timeline-description|project-description|skill-item" assets/css/mobile-optimizations.css` shows mobile dense-content overrides
- [ ] No unrelated shell, async-state, or homepage architecture selectors were changed outside readability scope
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Dense shared surfaces have a more comfortable reading floor on desktop
- Dense shared surfaces remain comfortable on mobile without losing hierarchy
  </success_criteria>

<output>
After completion, create `.planning/phases/03-readability-upgrade/03-01-SUMMARY.md`
</output>
