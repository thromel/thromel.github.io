---
phase: 07-navigation-and-metadata-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - tests/navbar-layout.spec.js
  - tests/shell-behavior.spec.js
  - scripts/verify-ui.sh
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
must_haves:
  truths:
    - "Navigation route grouping is covered by deterministic browser checks."
    - "Active state is checked on representative primary routes, including Publications and CV."
    - "Core page description sources are checked before Phase 10 SEO/social work starts."
  artifacts:
    - path: "tests/navbar-layout.spec.js"
      provides: "Primary/secondary navigation and active-state regression coverage"
      contains: "primaryRoutes"
    - path: "scripts/verify-ui.sh"
      provides: "Canonical runner includes Phase 7 navigation checks"
      contains: "navbar-layout.spec.js"
---

<objective>
Verify the Phase 7 navigation and metadata foundation.

Purpose: The phase should not close on source edits alone. This plan extends the shell tests so route grouping, active state, footer reachability, and page description sources are checked repeatably.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-navigation-and-metadata-foundation/07-RESEARCH.md
@.planning/phases/07-navigation-and-metadata-foundation/07-UI-SPEC.md
@.planning/phases/07-navigation-and-metadata-foundation/07-VALIDATION.md
@AGENTS.md
@tests/navbar-layout.spec.js
@tests/shell-behavior.spec.js
@scripts/verify-ui.sh
@_data/navigation.yml
@_includes/navbar.html
@_includes/footer.html
</context>

<threat_model>
Verification changes do not expose production data. The main risk is false confidence from tests that assert text but not behavior. Mitigate by checking header inclusion/exclusion, footer reachability, active-state routes, no horizontal overflow, and source metadata.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend navigation browser coverage</name>
  <files>tests/navbar-layout.spec.js, tests/shell-behavior.spec.js</files>
  <read_first>tests/navbar-layout.spec.js, tests/shell-behavior.spec.js, _data/navigation.yml, _includes/navbar.html, _includes/footer.html</read_first>
  <action>Update `tests/navbar-layout.spec.js` to assert the header contains Home, Research, Publications, Projects, Contributions, and CV, and does not contain About, Education, Work, Achievements, News, or Learning. Add footer assertions for the secondary route set. Add active-state checks for `/`, `/research`, `/publications`, `/projects`, `/contributions`, and `/cv`. Preserve the existing check that there is no `TR` marker. Keep the no-horizontal-overflow coverage in `tests/shell-behavior.spec.js`; expand its page cases only if needed for Publications and CV.</action>
  <verify>PLAYWRIGHT_TEST_BASE_URL="http://127.0.0.1:4000" npx playwright test tests/navbar-layout.spec.js tests/shell-behavior.spec.js</verify>
  <acceptance_criteria>
    - `tests/navbar-layout.spec.js` defines or asserts the six primary header routes
    - `tests/navbar-layout.spec.js` asserts secondary routes are available outside the header
    - active state is checked for Home, Research, Publications, Projects, Contributions, and CV
    - shell tests still assert no horizontal overflow on mobile
  </acceptance_criteria>
  <done>Navigation route grouping and active state have browser coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Add metadata source verification</name>
  <files>tests/navbar-layout.spec.js, scripts/verify-ui.sh</files>
  <read_first>tests/navbar-layout.spec.js, scripts/verify-ui.sh, index.html, research.html, publications.html, projects.html, contributions.html, cv.html, experience.html, about.html</read_first>
  <action>Add a deterministic check that the eight core pages contain `description:` front matter. Prefer a small Node `fs` check inside `tests/navbar-layout.spec.js` or a dedicated Playwright test block that reads source files; if that is too awkward, add a focused shell/source check to `scripts/verify-ui.sh shell`. The canonical shell runner must execute the metadata source check before Phase 7 closes.</action>
  <verify>bash scripts/verify-ui.sh shell</verify>
  <acceptance_criteria>
    - `bash scripts/verify-ui.sh shell` fails if any of the eight core pages lacks `description:` front matter
    - metadata verification does not require live network access
    - no Phase 10 Open Graph/Twitter preview output is required by this check
  </acceptance_criteria>
  <done>Metadata source readiness is part of the shell verification path.</done>
</task>

<task type="auto">
  <name>Task 3: Close Phase 7 roadmap and full verification evidence</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/07-navigation-and-metadata-foundation/07-VALIDATION.md, scripts/verify-ui.sh</read_first>
  <action>After Plans 07-01 through 07-03 are implemented, update the Phase 7 plan checklist in `.planning/ROADMAP.md` from drafted to complete only if `bash scripts/verify-ui.sh full` passes. Do not mark Phase 8 or later requirements complete. Record any verification limitation in the phase summary instead of silently marking success.</action>
  <verify>bash scripts/verify-ui.sh full</verify>
  <acceptance_criteria>
    - `bash scripts/verify-ui.sh full` passes before Phase 7 is marked complete
    - `.planning/ROADMAP.md` marks only Phase 7 plan entries complete
    - Phase 7 summary records the exact verification command and result
  </acceptance_criteria>
  <done>Phase 7 closes with full UI verification evidence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash scripts/verify-ui.sh shell`
- [ ] `bash scripts/verify-ui.sh full`
- [ ] `rg -n "primary_pages|secondary_pages|description:" _data/navigation.yml index.html research.html publications.html projects.html contributions.html cv.html experience.html about.html`
</verification>

<success_criteria>
- Navigation grouping has browser coverage
- Active-page highlighting has browser coverage
- Secondary routes remain reachable and tested
- Metadata source readiness is tested
- Full UI verification passes before phase completion
</success_criteria>

<output>
After completion, create `.planning/phases/07-navigation-and-metadata-foundation/07-03-SUMMARY.md`
</output>
