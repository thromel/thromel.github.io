---
phase: 07-navigation-and-metadata-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _data/navigation.yml
  - index.html
  - research.html
  - publications.html
  - projects.html
  - contributions.html
  - cv.html
  - experience.html
  - about.html
autonomous: true
requirements:
  - NAV-01
  - NAV-02
must_haves:
  truths:
    - "Header navigation uses a primary route set instead of the current full sitemap."
    - "Secondary routes remain represented in navigation data so they can be rendered outside the header."
    - "Core pages expose page-specific `description` front matter as the metadata source for Phase 10."
  artifacts:
    - path: "_data/navigation.yml"
      provides: "Primary and secondary route groups"
      contains: "primary_pages"
    - path: "publications.html"
      provides: "Page-specific metadata source for the Publications route"
      contains: "description:"
  key_links:
    - from: "_data/navigation.yml"
      to: "_includes/navbar.html"
      via: "Header route rendering consumes primary navigation data"
      pattern: "primary_pages"
    - from: "_data/navigation.yml"
      to: "_includes/footer.html"
      via: "Footer route rendering consumes secondary navigation data"
      pattern: "secondary_pages"
---

<objective>
Define the navigation and metadata source model for Phase 7.

Purpose: The implementation needs a clean data source before template changes. This plan splits the current flat navigation list into primary and secondary groups, adds Publications/CV to the primary route set, and adds page descriptions that Phase 10 can later emit as SEO/social metadata.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-navigation-and-metadata-foundation/07-RESEARCH.md
@.planning/phases/07-navigation-and-metadata-foundation/07-UI-SPEC.md
@.planning/phases/07-navigation-and-metadata-foundation/07-PATTERNS.md
@AGENTS.md
@_data/navigation.yml
@index.html
@research.html
@publications.html
@projects.html
@contributions.html
@cv.html
@experience.html
@about.html
</context>

<threat_model>
No user input, auth, payment, or backend surface is introduced. Main risks are broken navigation reachability and misleading metadata. Mitigate through source checks, route checks, and avoiding URL changes.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Split navigation data into primary and secondary route groups</name>
  <files>_data/navigation.yml</files>
  <read_first>_data/navigation.yml, _includes/navbar.html, _includes/footer.html, .planning/phases/07-navigation-and-metadata-foundation/07-UI-SPEC.md</read_first>
  <action>Update `_data/navigation.yml` so it defines `primary_pages` with Home, Research, Publications, Projects, Contributions, and CV in that order. Define `secondary_pages` with About, Education, Work, Achievements, News, and Learning in that order. Preserve `pages` only if needed for backward compatibility, but the canonical grouped route data must be `primary_pages` and `secondary_pages`. Each item must keep a stable `name` matching the target page `navbar_title` or page title and a `url` for the existing route. Do not rename URLs.</action>
  <verify>rg -n "primary_pages|secondary_pages|name: Publications|name: CV|url: /publications|url: /cv" _data/navigation.yml</verify>
  <acceptance_criteria>
    - `_data/navigation.yml` contains `primary_pages:`
    - `_data/navigation.yml` contains `secondary_pages:`
    - `primary_pages` includes Home, Research, Publications, Projects, Contributions, and CV
    - `secondary_pages` includes About, Education, Work, Achievements, News, and Learning
    - no existing route URL for these pages is changed
  </acceptance_criteria>
  <done>Navigation has a data model that supports a compact header and reachable secondary routes.</done>
</task>

<task type="auto">
  <name>Task 2: Add page-specific descriptions to core pages</name>
  <files>index.html, research.html, publications.html, projects.html, contributions.html, cv.html, experience.html, about.html</files>
  <read_first>index.html, research.html, publications.html, projects.html, contributions.html, cv.html, experience.html, about.html, _config.yml, .planning/research/SUMMARY.md</read_first>
  <action>Add one `description:` front-matter field to each core page listed in `files`. Descriptions must be one sentence, page-specific, and aligned with the current portfolio framing: software engineering, LLM systems, research, projects, open-source contributions, CV, work, and profile context. Do not add Open Graph, Twitter, canonical, or preview-image tags in this plan; those belong to Phase 10.</action>
  <verify>rg -n "^description:" index.html research.html publications.html projects.html contributions.html cv.html experience.html about.html</verify>
  <acceptance_criteria>
    - all eight listed pages contain exactly one top-level `description:` front-matter field
    - descriptions are page-specific and not the generic string `Personal Academic Homepage`
    - no Open Graph or Twitter meta tags are added to page bodies
    - `bundle exec jekyll build` succeeds after the front-matter changes
  </acceptance_criteria>
  <done>Core pages have metadata source text for Phase 10 without prematurely implementing social previews.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "primary_pages|secondary_pages" _data/navigation.yml` finds both route groups
- [ ] `rg -n "^description:" index.html research.html publications.html projects.html contributions.html cv.html experience.html about.html` finds descriptions on all core pages
- [ ] `bundle exec jekyll build` succeeds
</verification>

<success_criteria>
- Primary and secondary route groups exist in navigation data
- Core page description sources exist
- Existing route URLs remain unchanged
- Jekyll builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/07-navigation-and-metadata-foundation/07-01-SUMMARY.md`
</output>
