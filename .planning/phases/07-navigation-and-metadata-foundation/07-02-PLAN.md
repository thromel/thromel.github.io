---
phase: 07-navigation-and-metadata-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - _includes/navbar.html
  - _includes/footer.html
  - assets/css/overhaul.css
  - cv.html
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
must_haves:
  truths:
    - "The header renders only the approved primary route set."
    - "The footer renders secondary routes so no current major page becomes hidden."
    - "Active-page highlighting still uses stable route names and page front matter."
  artifacts:
    - path: "_includes/navbar.html"
      provides: "Primary academic header route rendering"
      contains: "primary_pages"
    - path: "_includes/footer.html"
      provides: "Secondary academic route row"
      contains: "secondary_pages"
  key_links:
    - from: "_data/navigation.yml"
      to: "_includes/navbar.html"
      via: "primary route rendering"
      pattern: "primary_pages"
    - from: "_data/navigation.yml"
      to: "_includes/footer.html"
      via: "secondary route rendering"
      pattern: "secondary_pages"
---

<objective>
Render the new navigation model through the shared academic shell.

Purpose: The data split only matters if the active templates use it. This plan updates the header to render primary routes, adds a footer row for secondary routes, and preserves active-state and theme-toggle behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-navigation-and-metadata-foundation/07-RESEARCH.md
@.planning/phases/07-navigation-and-metadata-foundation/07-UI-SPEC.md
@.planning/phases/07-navigation-and-metadata-foundation/07-PATTERNS.md
@AGENTS.md
@_data/navigation.yml
@_includes/navbar.html
@_includes/footer.html
@_layouts/default.html
@assets/css/overhaul.css
@assets/js/site-shell.js
@cv.html
</context>

<threat_model>
No new data trust boundary is introduced. Main risks are inaccessible routes, duplicated navigation controls, and broken keyboard/screen-reader state. Mitigate with semantic links, `aria-current`, one theme toggle, and Playwright shell checks.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Render primary routes in the header</name>
  <files>_includes/navbar.html, cv.html</files>
  <read_first>_includes/navbar.html, _data/navigation.yml, _layouts/default.html, cv.html, assets/js/site-shell.js</read_first>
  <action>Update `_includes/navbar.html` so the header loops over `site.data.navigation.primary_pages` and falls back to `site.data.navigation.pages` only if `primary_pages` is absent. Preserve `id="site-navigation"`, `class="academic-nav"`, slash separators, and one `button#themeToggle.theme-toggle`. Active state must still set `aria-current="page"` when `item.name == page.navbar_title`, with Home as the existing nil-title fallback. Add `navbar_title: CV` to `cv.html` so the CV primary route can be highlighted.</action>
  <verify>rg -n "primary_pages|site-navigation|themeToggle|aria-current|navbar_title: CV" _includes/navbar.html cv.html</verify>
  <acceptance_criteria>
    - `_includes/navbar.html` references `site.data.navigation.primary_pages`
    - `_includes/navbar.html` still renders `id="site-navigation"` and `class="academic-nav"`
    - `_includes/navbar.html` still contains exactly one `themeToggle` button
    - active-state logic still compares route `name` to `page.navbar_title`
    - `cv.html` contains `navbar_title: CV`
  </acceptance_criteria>
  <done>The header renders the compact primary route set and can highlight CV.</done>
</task>

<task type="auto">
  <name>Task 2: Render secondary routes in the footer</name>
  <files>_includes/footer.html, assets/css/overhaul.css</files>
  <read_first>_includes/footer.html, _data/navigation.yml, assets/css/overhaul.css, _layouts/default.html</read_first>
  <action>Update `_includes/footer.html` to render a second row from `site.data.navigation.secondary_pages`. Use semantic links, slash separators, and the existing academic footer style. Add only minimal CSS in `assets/css/overhaul.css` if needed, using class names such as `.academic-footer__links` or `.academic-footer__secondary`. Do not add a sitemap card, columns, dropdowns, JavaScript behavior, or another theme/navigation model.</action>
  <verify>rg -n "secondary_pages|academic-footer__|About|Education|Learning" _includes/footer.html assets/css/overhaul.css</verify>
  <acceptance_criteria>
    - `_includes/footer.html` references `site.data.navigation.secondary_pages`
    - footer secondary links include About, Education, Work, Achievements, News, and Learning through data rendering
    - `assets/css/overhaul.css` contains any new footer styles used by the include
    - no `.navbar`, drawer, dropdown, or Bootstrap utility dependency is introduced
  </acceptance_criteria>
  <done>Secondary routes remain visible and reachable after header cleanup.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "primary_pages|secondary_pages" _includes/navbar.html _includes/footer.html`
- [ ] `rg -n "navbar_title: CV" cv.html`
- [ ] `bash scripts/verify-ui.sh shell`
</verification>

<success_criteria>
- Header renders only primary navigation routes
- Footer renders secondary navigation routes
- Active-page highlighting works for primary routes
- Theme toggle behavior remains unchanged
- Shell verification passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-navigation-and-metadata-foundation/07-02-SUMMARY.md`
</output>
