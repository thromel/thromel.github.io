---
phase: 04-proof-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/overhaul.css
  - assets/css/components/oss-summary.css
  - assets/js/github-proof.js
autonomous: true
requirements:
  - OSS-01
  - OSS-03
user_setup: []
must_haves:
  truths:
    - "Proof surfaces share one explicit async-state presentation model instead of page-local one-offs."
    - "Homepage and contributions runtime code can switch `loading`, `success`, `empty`, and `error` states through shared helpers."
    - "Phase 4 builds on the canonical token system rather than reintroducing legacy styling vocabulary."
  artifacts:
    - path: "assets/css/overhaul.css"
      provides: "Canonical proof-surface and async-state styling hooks"
      contains: ".proof-surface"
    - path: "assets/css/components/oss-summary.css"
      provides: "Homepage proof-row component styling aligned with the shared async-state system"
      contains: ".proof-state"
    - path: "assets/js/github-proof.js"
      provides: "Reusable GitHub proof-state helper functions for homepage and contributions"
      contains: "setProofState"
  key_links:
    - from: "assets/js/github-proof.js"
      to: "assets/js/oss-summary.js"
      via: "homepage OSS summary uses the shared GitHub proof helper instead of page-only response handling"
      pattern: "setProofState|classifyGitHubStatus"
    - from: "assets/js/github-proof.js"
      to: "assets/js/contributions.js"
      via: "contributions runtime reuses the same failure classification and state transitions"
      pattern: "setProofState|normalizeRepositoryName"
---

<objective>
Create the shared presentation and runtime primitives that Phase 4 needs before the homepage OSS summary and contributions page can converge.

Purpose: Phase 4 should not solve homepage and contributions separately. It needs a shared proof-surface language for async states first.
Output: `assets/css/overhaul.css`, `assets/css/components/oss-summary.css`, and `assets/js/github-proof.js` provide the reusable CSS and JavaScript hooks that later page-specific changes will consume.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-proof-surfaces/04-RESEARCH.md
@.planning/phases/04-proof-surfaces/04-VALIDATION.md
@assets/css/overhaul.css
@assets/css/components/oss-summary.css
@assets/js/oss-summary.js
@contributions.html
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical proof-surface and async-state selectors to the shared styling layers</name>
  <files>assets/css/overhaul.css, assets/css/components/oss-summary.css</files>
  <read_first>assets/css/overhaul.css, assets/css/components/oss-summary.css, index.html, contributions.html, .planning/phases/04-proof-surfaces/04-RESEARCH.md</read_first>
  <action>Update `assets/css/overhaul.css` and `assets/css/components/oss-summary.css` to define a shared proof-surface vocabulary for GitHub-driven panels. Add selectors for `.proof-surface`, `.proof-surface-header`, `.proof-surface-metrics`, `.proof-links`, and `.proof-state`, plus explicit state variants for `.proof-state[data-state="loading"]`, `.proof-state[data-state="success"]`, `.proof-state[data-state="empty"]`, and `.proof-state[data-state="error"]`. Keep the styling on the current token system and card language already used by the homepage support band and timeline cards. Do not add a second color palette or page-specific legacy tokens in this task.</action>
  <verify>rg -n 'proof-surface|proof-state|data-state="loading"|data-state="success"|data-state="empty"|data-state="error"|proof-links|proof-surface-metrics' assets/css/overhaul.css assets/css/components/oss-summary.css</verify>
  <acceptance_criteria>
    - `assets/css/overhaul.css` contains `.proof-surface`
    - `assets/css/overhaul.css` contains `.proof-surface-metrics`
    - `assets/css/components/oss-summary.css` contains `.proof-state`
    - `assets/css/components/oss-summary.css` contains `data-state="loading"`
    - `assets/css/components/oss-summary.css` contains `data-state="error"`
    - `assets/css/components/oss-summary.css` contains `.proof-links`
  </acceptance_criteria>
  <done>The repo has a reusable async-state visual language that both proof surfaces can share.</done>
</task>

<task type="auto">
  <name>Task 2: Create a shared GitHub proof helper with concrete state and failure-classification functions</name>
  <files>assets/js/github-proof.js</files>
  <read_first>assets/js/oss-summary.js, contributions.html, .planning/phases/04-proof-surfaces/04-RESEARCH.md</read_first>
  <action>Create `assets/js/github-proof.js` and expose a small shared helper on `window.GitHubProof`. Implement concrete functions named `classifyGitHubStatus(response, bodyText)`, `setProofState(root, state)`, `setProofMessage(target, message)`, and `normalizeRepositoryName(repositoryUrl)`. `classifyGitHubStatus` must distinguish at least `success`, `empty`, `rate-limit`, and `error` outcomes so homepage and contributions can render useful fallback copy instead of swallowing failures. Keep the helper presentation-focused and state-oriented; do not move page-specific rendering markup into this shared file.</action>
  <verify>node -c assets/js/github-proof.js && rg -n 'window\\.GitHubProof|classifyGitHubStatus|setProofState|setProofMessage|normalizeRepositoryName|rate-limit' assets/js/github-proof.js</verify>
  <acceptance_criteria>
    - `assets/js/github-proof.js` exists
    - `assets/js/github-proof.js` contains `window.GitHubProof`
    - `assets/js/github-proof.js` contains `classifyGitHubStatus`
    - `assets/js/github-proof.js` contains `setProofState`
    - `assets/js/github-proof.js` contains `setProofMessage`
    - `assets/js/github-proof.js` contains `normalizeRepositoryName`
    - `node -c assets/js/github-proof.js` exits 0
  </acceptance_criteria>
  <done>The repo has one shared helper for proof-surface state transitions and GitHub failure classification.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n 'proof-surface|proof-state' assets/css/overhaul.css assets/css/components/oss-summary.css` shows the shared proof-state styling hooks
- [ ] `node -c assets/js/github-proof.js` exits 0
- [ ] `rg -n 'classifyGitHubStatus|setProofState|normalizeRepositoryName' assets/js/github-proof.js` shows the shared helper API
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Shared proof-surface styling exists in the canonical token system
- Shared GitHub proof helpers exist before homepage and contributions migrations begin
  </success_criteria>

<output>
After completion, create `.planning/phases/04-proof-surfaces/04-01-SUMMARY.md`
</output>
