---
phase: 04-proof-surfaces
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - index.html
  - contributions.html
  - assets/js/oss-summary.js
  - assets/js/contributions.js
autonomous: true
requirements:
  - OSS-01
  - OSS-02
  - OSS-03
user_setup: []
must_haves:
  truths:
    - "The homepage OSS summary stays visible as a coherent proof surface before and after GitHub data resolves."
    - "The contributions page uses explicit `loading`, `success`, `empty`, and `error` transitions through dedicated runtime code."
    - "Failure and rate-limit states still leave the visitor with useful proof-of-work context."
  artifacts:
    - path: "index.html"
      provides: "Stable homepage OSS summary shell with explicit proof-state hooks"
      contains: "oss-summary-status"
    - path: "assets/js/oss-summary.js"
      provides: "Homepage OSS runtime that drives explicit state transitions instead of hiding on failure"
      contains: "GitHub data is unavailable right now"
    - path: "contributions.html"
      provides: "Contributions markup aligned to the shared proof-surface system"
      contains: "loading-state"
    - path: "assets/js/contributions.js"
      provides: "Dedicated contributions runtime with explicit state management"
      contains: "setContributionsState"
  key_links:
    - from: "assets/js/oss-summary.js"
      to: "index.html"
      via: "homepage proof row uses explicit `loading`, `success`, `empty`, and `error` states in a persistent shell"
      pattern: "oss-summary|oss-summary-status|oss-summary-text"
    - from: "assets/js/contributions.js"
      to: "contributions.html"
      via: "contributions page moves off inline runtime and onto explicit state transitions with shared proof styling"
      pattern: "loading-state|error-state|empty-state|contributions-section"
---

<objective>
Modernize the homepage OSS summary and contributions page implementation onto the shared proof-surface system created in Plan 01.

Purpose: Phase 4 succeeds only if the two live GitHub-driven surfaces become reliable, explicit, and visually aligned under both success and failure conditions.
Output: `index.html`, `contributions.html`, `assets/js/oss-summary.js`, and `assets/js/contributions.js` drive stable proof-surface behavior through the shared state model.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-proof-surfaces/04-RESEARCH.md
@.planning/phases/04-proof-surfaces/04-VALIDATION.md
@assets/js/github-proof.js
@assets/js/oss-summary.js
@index.html
@contributions.html
@assets/css/overhaul.css
@assets/css/components/oss-summary.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the homepage success-only OSS fragment with an always-rendered proof surface</name>
  <files>index.html, assets/js/oss-summary.js</files>
  <read_first>index.html, assets/js/oss-summary.js, assets/js/github-proof.js, assets/css/components/oss-summary.css, .planning/phases/04-proof-surfaces/04-RESEARCH.md</read_first>
  <action>Update `index.html` so `#oss-summary` is always rendered inside the homepage support stack as a proof-surface shell instead of using `style="display: none;"`. Add explicit child hooks named `#oss-summary-status` and `#oss-summary-text`, keep the repository links visible inside a shared `.proof-links` region, and initialize the shell with `data-state="loading"`. Then update `assets/js/oss-summary.js` to load `window.GitHubProof`, fetch the merged pull-request count, and drive concrete `loading`, `success`, `empty`, and `error` states instead of hiding the block. When GitHub fails or rate-limits, render the exact fallback message `GitHub data is unavailable right now. Browse recent contribution targets instead.` while preserving the repo links.</action>
  <verify>rg -n 'oss-summary-status|data-state="loading"|proof-links|GitHub data is unavailable right now|setProofState|classifyGitHubStatus' index.html assets/js/oss-summary.js</verify>
  <acceptance_criteria>
    - `index.html` contains `id="oss-summary-status"`
    - `index.html` contains `data-state="loading"`
    - `index.html` contains `proof-links`
    - `assets/js/oss-summary.js` contains `setProofState`
    - `assets/js/oss-summary.js` contains `classifyGitHubStatus`
    - `assets/js/oss-summary.js` contains `GitHub data is unavailable right now. Browse recent contribution targets instead.`
  </acceptance_criteria>
  <done>The homepage proof row remains stable and useful before, during, and after GitHub fetch resolution.</done>
</task>

<task type="auto">
  <name>Task 2: Move contributions onto dedicated runtime code with explicit success, empty, and failure states</name>
  <files>contributions.html, assets/js/contributions.js</files>
  <read_first>contributions.html, assets/js/github-proof.js, assets/js/oss-summary.js, .planning/phases/04-proof-surfaces/04-RESEARCH.md</read_first>
  <action>Create `assets/js/contributions.js` and move the page's inline runtime out of `contributions.html`. Implement concrete functions named `setContributionsState(state, message)` and `renderContributionItem(pr)` in the new file. Keep the existing state anchors `loading-state`, `error-state`, `empty-state`, `contributions-section`, and `contributions-timeline`, but drive them through explicit state transitions using `window.GitHubProof`. Update `contributions.html` to load the new script with `{{ '/assets/js/contributions.js' | relative_url }}` and to apply the shared proof-surface classes around stats and state containers. Preserve the merged/open filtering and repo grouping, but add failure copy that distinguishes rate-limit/unavailable cases with the exact text `GitHub is rate-limited right now. Check back shortly or use the repository links below.` and generic failures with `Unable to fetch contributions right now. Please try again later.`</action>
  <verify>node -c assets/js/contributions.js && rg -n "setContributionsState|renderContributionItem|loading-state|error-state|empty-state|contributions-section|GitHub is rate-limited right now|Unable to fetch contributions right now|assets/js/contributions\\.js" assets/js/contributions.js contributions.html</verify>
  <acceptance_criteria>
    - `assets/js/contributions.js` exists
    - `assets/js/contributions.js` contains `setContributionsState`
    - `assets/js/contributions.js` contains `renderContributionItem`
    - `assets/js/contributions.js` contains `GitHub is rate-limited right now. Check back shortly or use the repository links below.`
    - `assets/js/contributions.js` contains `Unable to fetch contributions right now. Please try again later.`
    - `contributions.html` contains `assets/js/contributions.js`
    - `contributions.html` contains `loading-state`
    - `contributions.html` contains `contributions-section`
    - `node -c assets/js/contributions.js` exits 0
  </acceptance_criteria>
  <done>The contributions page uses dedicated runtime code and explicit state handling without losing its current data model.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n 'oss-summary-status|data-state="loading"|proof-links' index.html` shows the stable homepage proof shell
- [ ] `node -c assets/js/oss-summary.js` exits 0
- [ ] `node -c assets/js/contributions.js` exits 0
- [ ] `rg -n 'rate-limited|Unable to fetch contributions right now|setContributionsState' assets/js/oss-summary.js assets/js/contributions.js` shows explicit fallback handling
- [ ] No legacy inline contributions runtime remains in `contributions.html`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Homepage OSS summary no longer disappears silently on failure
- Contributions uses explicit, shared proof-state handling across loading, success, empty, and failure
  </success_criteria>

<output>
After completion, create `.planning/phases/04-proof-surfaces/04-02-SUMMARY.md`
</output>
