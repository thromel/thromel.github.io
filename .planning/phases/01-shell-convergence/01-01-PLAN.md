---
phase: 01-shell-convergence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _layouts/default.html
  - assets/js/site-shell.js
autonomous: true
requirements:
  - UI-01
  - UI-02
user_setup: []
must_haves:
  truths:
    - "First-visit theme selection follows the browser's `prefers-color-scheme` value until the visitor clicks the shared theme toggle."
    - "The shared theme toggle updates `<html data-theme>` and persists only an explicit visitor choice in `localStorage.theme`."
    - "The canonical shell runtime still initializes the mobile drawer, scroll progress bar, back-to-top button, and homepage section navigation from `assets/js/site-shell.js`."
  artifacts:
    - path: "_layouts/default.html"
      provides: "Inline theme bootstrap aligned to the canonical `data-theme` model"
      contains: "prefers-color-scheme"
    - path: "assets/js/site-shell.js"
      provides: "Canonical shell runtime with non-persistent first-load theme resolution"
      contains: "themeChanged"
      min_lines: 120
  key_links:
    - from: "_layouts/default.html"
      to: "assets/js/site-shell.js"
      via: "shared `theme` localStorage key and `data-theme` bootstrap"
      pattern: "data-theme"
    - from: "assets/js/site-shell.js"
      to: "#themeToggle"
      via: "click handler that toggles and persists visitor-selected theme"
      pattern: "themeToggle"
---

<objective>
Make the canonical theme/bootstrap runtime the only live shell controller.

Purpose: Phase 1 cannot converge the shell while first-load theme behavior still forces dark mode and writes a default override into storage.
Output: `_layouts/default.html` and `assets/js/site-shell.js` resolve theme from one canonical `data-theme` model and keep the existing shell affordances alive.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shell-convergence/01-CONTEXT.md
@.planning/phases/01-shell-convergence/01-RESEARCH.md
@_layouts/default.html
@_includes/navbar.html
@_includes/footer.html
@assets/js/site-shell.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the forced-dark inline bootstrap with system-preference resolution</name>
  <files>_layouts/default.html</files>
  <read_first>_layouts/default.html, assets/js/site-shell.js, .planning/phases/01-shell-convergence/01-CONTEXT.md</read_first>
  <action>Replace the inline theme bootstrap in `_layouts/default.html` so it resolves the initial `data-theme` value in this exact order: a stored `localStorage.theme` value of `dark` or `light`, otherwise `window.matchMedia('(prefers-color-scheme: dark)')`, otherwise fallback `dark`. Keep the bootstrap inline in the `<head>` to avoid first-paint flicker. Do not add or restore `dark-theme` / `light-theme` root classes, and do not write a fallback theme into `localStorage` during bootstrap.</action>
  <verify>rg -n "prefers-color-scheme|localStorage.getItem\\('theme'\\)|setAttribute\\('data-theme'" _layouts/default.html</verify>
  <acceptance_criteria>
    - `_layouts/default.html` contains `prefers-color-scheme`
    - `_layouts/default.html` contains `setAttribute('data-theme'`
    - `_layouts/default.html` does not contain `setAttribute('data-theme', 'dark')` as the unconditional no-storage branch
    - `_layouts/default.html` does not contain `classList.add(\`\${theme}-theme\`)`
  </acceptance_criteria>
  <done>The inline bootstrap sets only `data-theme`, follows stored override or system preference, and avoids persisting a default value.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor the shell runtime so only explicit user toggles persist theme choice</name>
  <files>assets/js/site-shell.js</files>
  <read_first>assets/js/site-shell.js, _layouts/default.html, _includes/navbar.html, _includes/footer.html</read_first>
  <action>Refactor `assets/js/site-shell.js` to separate theme resolution, theme application, and persistence. Introduce a helper that resolves the current theme from `localStorage.theme` or `matchMedia('(prefers-color-scheme: dark)')`. Update `initTheme()` to apply the resolved theme without writing storage. Update `toggleTheme()` so it writes `localStorage.theme` only after a visitor click. Keep the root theme state as `<html data-theme="dark|light">`, preserve the existing `#themeToggle`, `#mobileMenuToggle`, `#mobileNavDrawer`, `#mobileNavOverlay`, `#scrollProgress`, and `#backToTop` behavior, and dispatch `new CustomEvent('themeChanged', { detail: { theme } })` after changing theme so future theme-aware utilities can react to the canonical shell event.</action>
  <verify>rg -n "matchMedia\\('\\(prefers-color-scheme: dark\\)'\\)|themeChanged|window\\.siteUX|mobileNavDrawer|backToTop" assets/js/site-shell.js</verify>
  <acceptance_criteria>
    - `assets/js/site-shell.js` contains `matchMedia('(prefers-color-scheme: dark)')`
    - `assets/js/site-shell.js` contains `new CustomEvent('themeChanged'`
    - `assets/js/site-shell.js` contains `window.siteUX =`
    - `assets/js/site-shell.js` does not contain `DEFAULT_THEME = 'dark'`
    - `assets/js/site-shell.js` still contains `initMobileNav`, `initScrollProgress`, and `initBackToTop`
  </acceptance_criteria>
  <done>The canonical shell runtime resolves first-load theme without persisting defaults and still owns all shared shell behaviors.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "prefers-color-scheme|data-theme" _layouts/default.html assets/js/site-shell.js` shows the canonical bootstrap/runtime path
- [ ] `rg -n "dark-theme|light-theme" _layouts/default.html assets/js/site-shell.js` returns no matches
- [ ] Theme toggle still exists in `_layouts/default.html` and `window.siteUX` still exists in `assets/js/site-shell.js`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No root-class theme model remains in the active shell bootstrap/runtime
- The canonical shell still owns theme toggle, nav drawer, progress bar, back-to-top, and home section nav behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/01-shell-convergence/01-01-SUMMARY.md`
</output>
