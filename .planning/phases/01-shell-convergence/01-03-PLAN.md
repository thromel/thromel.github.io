---
phase: 01-shell-convergence
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - tests/navbar-layout.spec.js
  - tests/shell-behavior.spec.js
  - browser-console-tests.js
  - manual-testing-script.md
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
user_setup: []
must_haves:
  truths:
    - "Maintainer can verify the shared shell on Home, About, and Contributions at desktop and mobile widths."
    - "Automated shell checks fail if duplicate theme toggles appear or if canonical shell IDs disappear."
    - "Manual shell smoke guidance explicitly covers theme persistence, skip links, nav drawer behavior, scroll progress, and back-to-top behavior on representative pages."
  artifacts:
    - path: "tests/navbar-layout.spec.js"
      provides: "Desktop shell layout regression coverage"
      contains: "site-navigation"
    - path: "tests/shell-behavior.spec.js"
      provides: "Representative page shell regression coverage for theme and mobile nav"
      contains: "mobileNavDrawer"
      min_lines: 40
    - path: "browser-console-tests.js"
      provides: "Console smoke checks for canonical shell IDs and theme behavior"
      contains: "themeToggle"
    - path: "manual-testing-script.md"
      provides: "Human smoke checklist for shell regression checks"
      contains: "Contributions"
  key_links:
    - from: "tests/shell-behavior.spec.js"
      to: "_layouts/default.html"
      via: "Playwright assertions target canonical shell IDs and theme toggle behavior"
      pattern: "themeToggle|mobileMenuToggle|mobileNavDrawer|mobileNavOverlay"
    - from: "browser-console-tests.js"
      to: "assets/js/site-shell.js"
      via: "smoke tests assert `window.siteUX` and canonical shell IDs"
      pattern: "siteUX|themeToggle"
---

<objective>
Add representative regression checks for the canonical shell after the runtime and page-island cleanup lands.

Purpose: Phase 1 should finish with repeatable evidence that the shared shell still works across desktop and mobile pages, without waiting for the broader verification-hardening phase.
Output: Expanded Playwright shell checks, refreshed browser-console smoke coverage, and an updated manual shell smoke script.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shell-convergence/01-CONTEXT.md
@.planning/phases/01-shell-convergence/01-RESEARCH.md
@tests/navbar-layout.spec.js
@browser-console-tests.js
@manual-testing-script.md
@_layouts/default.html
@_includes/navbar.html
@_includes/footer.html
@assets/js/site-shell.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add representative Playwright coverage for canonical shell behavior on Home, About, and Contributions</name>
  <files>tests/navbar-layout.spec.js, tests/shell-behavior.spec.js</files>
  <read_first>tests/navbar-layout.spec.js, _layouts/default.html, _includes/navbar.html, _includes/footer.html, assets/js/site-shell.js</read_first>
  <action>Keep `tests/navbar-layout.spec.js` as the desktop navbar regression and add a new `tests/shell-behavior.spec.js` that checks canonical shell behavior on `/`, `/about`, and `/contributions`. Use desktop (`1280x720`) and mobile (`390x844`) viewports. Assert that each page has exactly one `#themeToggle`, that clicking it changes `<html data-theme>`, that a reload preserves the clicked theme, and that mobile navigation opens from `#mobileMenuToggle`, exposes `#mobileNavDrawer`, and closes through the overlay or close button. Use the existing canonical IDs from `_includes/navbar.html` and `_layouts/default.html`; do not write tests against legacy `.navbar` selectors.</action>
  <verify>rg -n "themeToggle|data-theme|mobileMenuToggle|mobileNavDrawer|mobileNavOverlay|390, 844|1280, 720" tests/navbar-layout.spec.js tests/shell-behavior.spec.js</verify>
  <acceptance_criteria>
    - `tests/shell-behavior.spec.js` exists
    - `tests/shell-behavior.spec.js` contains `themeToggle`
    - `tests/shell-behavior.spec.js` contains `mobileMenuToggle`
    - `tests/shell-behavior.spec.js` contains `mobileNavDrawer`
    - `tests/shell-behavior.spec.js` contains `/about`
    - `tests/shell-behavior.spec.js` contains `/contributions`
    - `tests/navbar-layout.spec.js` still contains `site-navigation`
  </acceptance_criteria>
  <done>The repo has representative automated shell checks for theme and mobile navigation across the key Phase 1 pages.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh console and manual smoke checks around the canonical shell contract</name>
  <files>browser-console-tests.js, manual-testing-script.md</files>
  <read_first>browser-console-tests.js, manual-testing-script.md, _layouts/default.html, assets/js/site-shell.js</read_first>
  <action>Update `browser-console-tests.js` so it explicitly validates the canonical shell contract: one `#themeToggle`, presence of `window.siteUX`, canonical nav IDs, and observable `data-theme` state. Update `manual-testing-script.md` so the manual shell smoke list explicitly covers Home, About, and Contributions; theme toggle persistence; skip links; mobile drawer open/close flows; scroll progress; and back-to-top behavior. Keep both artifacts lightweight and focused on Phase 1 shell checks rather than the later Phase 4 async-state redesign.</action>
  <verify>rg -n "data-theme|themeToggle|siteUX|About|Contributions|mobile drawer|skip links|back-to-top|progress" browser-console-tests.js manual-testing-script.md</verify>
  <acceptance_criteria>
    - `browser-console-tests.js` contains `data-theme`
    - `browser-console-tests.js` contains `themeToggle`
    - `browser-console-tests.js` contains `siteUX`
    - `manual-testing-script.md` contains `About`
    - `manual-testing-script.md` contains `Contributions`
    - `manual-testing-script.md` contains `theme persists`
    - `manual-testing-script.md` contains `back-to-top`
  </acceptance_criteria>
  <done>Both smoke-check artifacts point at the canonical shell contract and representative Phase 1 pages.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "themeToggle|siteUX|site-navigation|mobileMenuToggle|mobileNavDrawer|mobileNavOverlay|data-theme" tests/navbar-layout.spec.js tests/shell-behavior.spec.js browser-console-tests.js` shows canonical shell coverage
- [ ] `rg -n "Home|About|Contributions|theme persists|skip links|mobile drawer|back-to-top|progress" manual-testing-script.md` shows the representative page matrix and shell smoke steps
- [ ] `npx playwright test tests/navbar-layout.spec.js tests/shell-behavior.spec.js` is documented as the automated shell regression command
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The repo has representative desktop and mobile shell regression coverage for Home, About, and Contributions
- Phase 1 finishes with both automated and manual shell checks tied to the canonical shell vocabulary
  </success_criteria>

<output>
After completion, create `.planning/phases/01-shell-convergence/01-03-SUMMARY.md`
</output>
