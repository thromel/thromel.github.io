---
phase: 01-shell-convergence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - about.html
  - contributions.html
  - _includes/critical-css.html
autonomous: true
requirements:
  - UI-01
  - UI-03
user_setup: []
must_haves:
  truths:
    - "About and Contributions render under the same `data-theme` model as the shared shell without relying on `:root.dark-theme` or `:root.light-theme`."
    - "Top-level page-specific cards and state containers on shared pages use canonical token names from `assets/css/overhaul.css` instead of undefined legacy `--glass-*` or `--font-size-*` variables."
    - "Any retained shell-adjacent include CSS no longer carries `.navbar`-era selectors that would conflict with `.site-header` / `.site-nav` markup."
  artifacts:
    - path: "about.html"
      provides: "Canonical theme selectors and token names for the About page"
      contains: "[data-theme=\"light\"]"
    - path: "contributions.html"
      provides: "Canonical tokenized inline state styling for the Contributions page"
      contains: "--color-"
    - path: "_includes/critical-css.html"
      provides: "Dormant-safe critical CSS using canonical shell vocabulary"
      contains: ".site-header"
  key_links:
    - from: "about.html"
      to: "_layouts/default.html"
      via: "page styles now respond to `<html data-theme>` instead of root theme classes"
      pattern: "data-theme"
    - from: "contributions.html"
      to: "assets/css/overhaul.css"
      via: "inline page styles reference canonical `--color-*`, `--text-*`, `--space-*`, and `--radius-*` tokens"
      pattern: "--color-"
    - from: "_includes/critical-css.html"
      to: "_includes/navbar.html"
      via: "shared shell include selectors match `.site-header`, `.site-nav`, and `.skip-link` vocabulary"
      pattern: "site-header|site-nav"
---

<objective>
Remove the live top-level shell leaks that still depend on the old theme and token vocabulary.

Purpose: Phase 1 needs shared pages to follow the same theme model and token language as the canonical shell, but it should not absorb the broader async-state redesign reserved for later phases.
Output: About, Contributions, and the retained critical-css include stop relying on root theme classes, `.navbar` selectors, and legacy token names.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shell-convergence/01-CONTEXT.md
@.planning/phases/01-shell-convergence/01-RESEARCH.md
@about.html
@contributions.html
@_includes/critical-css.html
@assets/css/overhaul.css
@_layouts/default.html
@_includes/navbar.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite About page styles to the canonical theme and token model</name>
  <files>about.html</files>
  <read_first>about.html, assets/css/overhaul.css, _layouts/default.html</read_first>
  <action>Update the inline `<style>` block in `about.html` so it no longer uses `:root.light-theme`, `:root.dark-theme`, `--glass-bg`, `--glass-border`, `--font-size-*`, `--accent-primary`, `--accent-secondary`, `--text-muted`, or other legacy variable names. Replace them with `[data-theme=\"light\"]` / `[data-theme=\"dark\"]` selectors and canonical `overhaul.css` tokens such as `--color-surface-raised`, `--color-border`, `--color-accent`, `--color-text-primary`, `--color-text-secondary`, `--color-text-muted`, `--text-*`, `--space-*`, and `--radius-*`. Keep the existing About page structure, hover behavior, and copy intact.</action>
  <verify>rg -n "\\[data-theme=|--color-|--text-|--space-|--radius-" about.html</verify>
  <acceptance_criteria>
    - `about.html` contains `[data-theme="light"]` or `[data-theme='light']`
    - `about.html` contains `[data-theme="dark"]` or `[data-theme='dark']`
    - `about.html` does not contain `:root.light-theme`
    - `about.html` does not contain `:root.dark-theme`
    - `about.html` does not contain `--glass-bg`
    - `about.html` does not contain `--font-size-`
  </acceptance_criteria>
  <done>The About page responds to the canonical theme model and uses canonical token names only.</done>
</task>

<task type="auto">
  <name>Task 2: Convert Contributions page shell-adjacent inline styling to canonical tokens without redesigning the data flow</name>
  <files>contributions.html</files>
  <read_first>contributions.html, assets/css/overhaul.css, .planning/phases/01-shell-convergence/01-CONTEXT.md</read_first>
  <action>Update the inline `<style>` block in `contributions.html` so the stats cards, loading state, error state, empty state, labels, and metadata use canonical `overhaul.css` tokens: `--color-surface-raised`, `--color-border`, `--color-accent`, `--color-text-primary`, `--color-text-secondary`, `--color-text-muted`, `--text-*`, `--space-*`, and `--radius-*`. Keep the existing fetch/render logic, IDs (`loading-state`, `error-state`, `empty-state`, `contributions-section`), and GitHub API behavior unchanged. Do not redesign the contribution timeline or async-state architecture beyond the token/selector cleanup needed for shell convergence.</action>
  <verify>rg -n -- "--color-|--text-|--space-|--radius-|loading-state|error-state|empty-state|contributions-section" contributions.html</verify>
  <acceptance_criteria>
    - `contributions.html` contains `loading-state`, `error-state`, `empty-state`, and `contributions-section`
    - `contributions.html` contains at least one `--color-` token reference in the inline styles
    - `contributions.html` does not contain `--glass-bg`
    - `contributions.html` does not contain `--glass-border`
    - `contributions.html` does not contain `--font-size-`
    - `contributions.html` still contains `fetch(` and `document.addEventListener('DOMContentLoaded', fetchContributions)`
  </acceptance_criteria>
  <done>The Contributions page keeps its current data flow but stops depending on the old token vocabulary.</done>
</task>

<task type="auto">
  <name>Task 3: Make retained critical CSS safe for the canonical shell vocabulary</name>
  <files>_includes/critical-css.html</files>
  <read_first>_includes/critical-css.html, _includes/navbar.html, assets/css/overhaul.css</read_first>
  <action>Refactor `_includes/critical-css.html` so it no longer encodes `.navbar`, `.navbar-brand`, `:root.dark-theme`, or `:root.light-theme`. If the file is retained, rewrite it to the canonical shell vocabulary: `.site-header`, `.site-nav`, `.skip-link`, `.scroll-progress`, `.theme-toggle`, and `[data-theme=\"dark\"]` / `[data-theme=\"light\"]`. Remove the legacy fixed-navbar `body { padding-top: 60px; }` assumption so the dormant include cannot reintroduce the old layout model if it is ever re-used.</action>
  <verify>rg -n "\\.site-header|\\.site-nav|\\[data-theme=|\\.skip-link|\\.theme-toggle|\\.scroll-progress" _includes/critical-css.html</verify>
  <acceptance_criteria>
    - `_includes/critical-css.html` contains `.site-header`
    - `_includes/critical-css.html` contains `.site-nav`
    - `_includes/critical-css.html` contains `[data-theme="dark"]` or `[data-theme='dark']`
    - `_includes/critical-css.html` does not contain `.navbar`
    - `_includes/critical-css.html` does not contain `.navbar-brand`
    - `_includes/critical-css.html` does not contain `:root.dark-theme`
    - `_includes/critical-css.html` does not contain `:root.light-theme`
  </acceptance_criteria>
  <done>The dormant critical-css include cannot silently reintroduce the old shell vocabulary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n ":root\\.dark-theme|:root\\.light-theme|\\.navbar\\b|--glass-bg|--glass-border|--font-size-" about.html contributions.html _includes/critical-css.html` returns no matches
- [ ] `rg -n "\\[data-theme=|--color-|--text-|--space-|--radius-|\\.site-header|\\.site-nav" about.html contributions.html _includes/critical-css.html` shows canonical selectors/tokens
- [ ] `contributions.html` still contains the existing GitHub fetch flow and DOMContentLoaded bootstrap
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Live top-level pages no longer depend on the old root-class theme model
- Shared-page styling reads from the canonical token vocabulary instead of undefined legacy variables
  </success_criteria>

<output>
After completion, create `.planning/phases/01-shell-convergence/01-02-SUMMARY.md`
</output>
